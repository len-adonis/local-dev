# Prefer Vim
export VISUAL=vim
export EDITOR="$VISUAL"

# PATH
export PATH="$HOME/.local/bin:$PATH"
export PATH="/opt/homebrew/opt/libpq/bin:$PATH"
export PATH="/opt/homebrew/bin:/opt/homebrew/sbin:$PATH"

# Homebrew
# eval "$(/opt/homebrew/bin/brew shellenv)"
export HOMEBREW_PREFIX="/opt/homebrew"
export HOMEBREW_CELLAR="/opt/homebrew/Cellar"
export HOMEBREW_REPOSITORY="/opt/homebrew"

# Clean, Single Mise Activation
if [ -n "$ZSH_VERSION" ]; then
  # Zsh will handle mise asynchronously via zsh-defer inside .zshrc!
  # Leave this blank so Zsh doesn't stall here.
else
  # Mise (so bd/beads and other tools work in bash too)
  if [ -x "$HOME/.local/bin/mise" ]; then
    eval "$($HOME/.local/bin/mise activate bash)"
  else
    command -v mise >/dev/null && eval "$(mise activate bash)"
  fi
fi

# Worktrees from brtkwr.com/posts/2025-12-17-git-worktree-fzf-helper
# Can add it back
